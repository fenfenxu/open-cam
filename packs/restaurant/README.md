# 餐饮门店方案包

包含规则：

- **后厨区域入侵**（zone_intrusion）：有人进入后厨区域（默认画面左侧）触发，非工作时段尤其有用。
- **用餐区客流统计**（object_count）：画面内 person ≥ 20 触发，发现异常拥挤。
- **明火区域人员检测**（zone_intrusion）：有人进入灶台明火区域（默认画面左中小区域）触发。

应用后请在 Rules 页按店内实际布局调整多边形区域。
